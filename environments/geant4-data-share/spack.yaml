spack:
  include:
    - compilers.yaml
  config:
    install_tree:
      root: /home/wonyongc/src/fcc/share
      projections:
        all: "data/{name}-{version}-{hash}"
  view: false
  packages:
    all:
      variants: build_type=Release
  specs:
  - geant4-data@10.7.0
  - geant4-data@10.7.1
